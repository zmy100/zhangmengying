1.有序列表需要用到哪个标签, 无序列表需要用到哪个标签, 有序列表和无需列表有什么区别?
有序列表使用<ol>标签，无序列表使用<ul>标签。
在有序列表中，每个列表项都有一个数字或字母来标识其序号。而在无序列表中，每个列表项以一个特定的符号（如实心圆点或实心方块）来标识，没有明确的序号。

2.什么是定义列表, 有哪些标签? 标签的关系?
定义列表是一种用来展示名词及其定义的列表。每一项包含两个部分：一个术语或名词（定义术语），后面跟着对该术语的定义（叫做定义列表项）。
HTML中定义列表由 <dl>、<dt> 和 <dd> 三种标签组成。
<dl> 标签表示整个定义列表，其内部可包含多个 <dt>（定义术语）和 <dd>（定义列表项）。<dt> 用来表示术语，通常加粗显示。而 <dd> 用来表示术语的定义，一般是缩进显示。定义列表中的 <dt> 和 <dd> 标签是相互关联的。每一个 <dt> 表示一个术语或名词，它后面紧跟着对该术语的定义，而这个对应的定义则必须紧接着后面的 <dd> 中。

3.创建表格, 需要用到哪些标签, 这些标签的含义是什么, 作用是什么?
创建表格需要用到以下几个标签：
<table>：表示整个表格。
<thead>：表示表格头部（通常包含表格标题）。
<tbody>：表示表格主体（通常包含表格数据）。
<tfoot>：表示表格尾部（通常包含表格统计信息）。
<tr>：表示表格中的一行。
<th>：表示表格中的表头单元格（通常加粗居中显示）。
<td>：表示表格中的数据单元格。
<table> 是整个表格的容器。<thead>、<tbody> 和 <tfoot> 标签是用来分隔表格的不同部分以便更好地展示数据。<tr> 是表格中的行标签，它在 <table>、<thead>、<tbody> 和 <tfoot> 中都可以使用。而 <th> 和 <td> 则是表示表格中的单元格，其中 <th> 表示表头单元格，而 <td> 则表示数据单元格。

4.td 标签和 th 标签有什么区别?
<th>：表示表格中的表头单元格，用于表示该列或行的标题。通常会加粗显示，并居中对齐。
<td>：表示表格中的数据单元格，用于表示该列或行的数据。